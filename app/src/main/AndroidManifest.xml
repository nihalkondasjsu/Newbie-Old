<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nihalkonda.newbie">

    <application
        android:allowBackup="true"
        android:icon="@drawable/newbie"
        android:label="@string/app_name"
        android:roundIcon="@drawable/newbie"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".app.ProcedureGroupActivity"></activity>
        <activity android:name=".PermissionCheckActivity" />
        <activity android:name=".app.ProcessInstructionsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.REORDER_TASKS" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>